SELECT "metric" as "metric",  "lat", "lng", "connid" FROM "geometric" where time > now() - 24h
#SELECT "metric" as "metric",  "lat", "lng", "connid" FROM "geometric" where time > now() - 1h and "deployment"= 'martins'
